Issue #897 remediation evidence, material head bf55ebce47e32220b741f011f6fa79463d7862ad
Base develop: 6b735b34fdfbe46c3c65856aba62a1c44830a729

Independent findings remediated:
A-001 Meta/WhatsApp provider metering
- server/modules/usageGovernance/providerUsage.ts records accepted WhatsApp Cloud API messages in billingUsageEvents with provider=meta, channel=whatsapp, beneficiary/payer/sponsor attribution and no raw conversational content.
- server/modules/whatsapp/replyTransport.ts meters central logical outbound sends including successful fallback delivery.
- server/modules/whatsapp/processingAcknowledgementDelivery.ts meters successful processing acknowledgements.
- server/modules/usageGovernance/providerUsage.test.ts attacks sponsorship attribution and idempotency when a reprocess changes original/fallback outcome.
- Stable Meta idempotency is derived from opaque inbound/lifecycle source plus logical response position, not provider outcome.

A-002 estimated-to-effective cost reconciliation
- drizzle/0045_billing_usage_cost_reconciliation.sql persists immutable reconciliation history.
- server/repositories/usageCostReconciliationRepository.ts uses FOR UPDATE, rejects currency mismatch and reconciliation-key conflicts, preserves previous estimated/effective values, and updates effectiveCostMicros only after the audit row is inserted in the same transaction.
- server/modules/usageGovernance/costReconciliation.ts refreshes affected daily and monthly aggregates only for a newly-applied reconciliation.
- server/modules/usageGovernance/costReconciliation.test.ts attacks duplicate reconciliation and derived-state recomputation.
- usage_cost_correction was removed from the admin economic-fact route so cost correction cannot bypass the auditable reconciliation contract.

A-003 configurable fair-use thresholds
- existing billingUsagePolicies storage is now active through usageGovernancePolicyRepository.ts.
- policyService.ts validates exactly three strictly increasing thresholds, positive budget, currency and observation window, and preserves automaticBlockingAtBudgetThreshold=false.
- router.ts exposes configurePolicy only as adminProcedure and analytics resolves persisted user/global policy before default fallback.
- policyService.test.ts verifies persisted thresholds override defaults and configuration remains non-blocking.

Historical independent findings remain covered by the previous implementation controls: charge authorization rollback and history attribution, rolling economic series/currency separation, runtime exemption and legal hold, bounded authorization chains, exact competence allocation, and retroactive recomposition.

Remote snapshot after final material push: GitHub Actions workflows for Agent-first, WhatsApp context TiDB, Billing persistence TiDB, Professional persistence TiDB and Onboarding activation TiDB were queued; AI provider live smoke was skipped by its workflow conditions. CI is remote evidence and is not treated as independent audit approval.

Post-handoff CI remediation:
- Agent-first on material 252c2a0a... failed because providerUsage.ts imported the full billing service at module load, which required logPersistenceWarning from ../../db inside unrelated healthIntegrations tests that partially mock db.
- Material bf55ebce... removes the top-level billing import and loads billingService lazily only inside recordMetaWhatsAppOutboundUsage after a user is resolved. This keeps the WhatsApp transport import boundary independent from billing persistence initialization while preserving runtime attribution behavior.
- The fix is intentionally in production dependency structure; unrelated healthIntegrations mocks were not weakened or patched.
