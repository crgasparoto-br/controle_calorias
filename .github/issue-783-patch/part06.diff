+    captureMealBatchHabitSnapshotMock.mockResolvedValue([{ foodName: "Histórico", preferredPortionGrams: 30, occurrenceCount: 7, lastSeenAt: 1 }]);
   });
@@ -113,6 +116,8 @@ describe("meal batch mutation compensation", () => {
       finalizeBatch: expect.objectContaining({
         recordCatalogUsage: false,
+        restoreHabits: true,
+        habitSnapshot: expect.arrayContaining([expect.objectContaining({ foodName: "Histórico", occurrenceCount: 7 })]),
         throwOnHabitFailure: true,
       }),
@@ -137,7 +142,10 @@ describe("meal batch mutation compensation", () => {
       updateHabits: false,
       logEvent: false,
-      finalizeBatch: expect.objectContaining({ meals: expect.any(Array) }),
+      finalizeBatch: expect.objectContaining({
+        meals: expect.any(Array),
+        habitSnapshot: expect.arrayContaining([expect.objectContaining({ occurrenceCount: 7 })]),
+        throwOnHabitFailure: true,
+      }),
     });
   });
diff --git a/server/whatsappAnnotatedImageWebhook.test.ts b/server/whatsappAnnotatedImageWebhook.test.ts
index 099d7b2..0000000 100644
--- a/server/whatsappAnnotatedImageWebhook.test.ts
+++ b/server/whatsappAnnotatedImageWebhook.test.ts
@@ -302,6 +302,38 @@ describe("handleWhatsAppWebhookWithTextIntent annotated image flow", () => {
     expect(markMessageProcessedMock).toHaveBeenCalledWith({ conversationId: 1, messageId: 1 });
   });
+
+  it("renderiza o estado persistido recarregado em vez do payload anterior à gravação", async () => {
+    const persistedMeal = {
+      ...savedImageMeal,
+      items: [{
+        ...savedImageMeal.items[0],
+        foodName: "Arroz persistido",
+        portionText: "120 g",
+        estimatedGrams: 120,
+        calories: 156,
+      }],
+    };
+    listUserMealsMock
+      .mockResolvedValueOnce([savedImageMeal])
+      .mockResolvedValueOnce([persistedMeal]);
+
+    const req = createImageWebhookRequest("image-persisted-state");
+    const res = createResponse();
+
+    await handleWhatsAppWebhookWithTextIntent(req as never, res as never);
+
+    const functionalText = sentTextMessages[0] ?? "";
+    expect(functionalText).toContain("Arroz persistido");
+    expect(functionalText).toContain("120g");
+    expect(functionalText).toContain("156 kcal");
+  });
 
   it("consolida a foto na refeição lógica existente do mesmo dia", async () => {
diff --git a/server/modules/whatsapp/intent/issue783.atomicity.test.ts b/server/modules/whatsapp/intent/issue783.atomicity.test.ts
index 809f5e1..0000000 100644
--- a/server/modules/whatsapp/intent/issue783.atomicity.test.ts
+++ b/server/modules/whatsapp/intent/issue783.atomicity.test.ts
@@ -58,6 +58,35 @@ describe("issue #783 — atomicidade e identidade de candidatos", () => {
     expect(result.action).toBe("clarification_needed");
   });
+
+  it("preserva uma ação clara posterior à ambiguidade na posição original do plano", async () => {
+    listMealsMock.mockResolvedValue([
+      { id: 10, mealLabel: "Jantar", occurredAt: "2026-07-12T22:00:00.000Z", items: [item("Queijo minas", 50), item("Arroz branco", 100)] },
+      { id: 20, mealLabel: "Almoço", occurredAt: "2026-07-12T16:00:00.000Z", items: [item("Queijo mussarela", 40)] },
+    ]);
+
+    await handleMealItemMultiIncrement(42, [
+      { targetFood: "queijo", gramsDelta: 10 },
+      { targetFood: "arroz", gramsDelta: 20 },
+    ]);
+
+    const pending = createPendingMealItemSelectionMock.mock.calls[0][1];
+    expect(pending.companionActions ?? []).toEqual([]);
+    expect(pending.remainingSelections).toEqual([
+      expect.objectContaining({
+        kind: "resolved",
+        candidate: expect.objectContaining({ mealId: 10, itemName: "Arroz branco" }),
+        action: { kind: "grams_delta", delta: 20 },
+      }),
+    ]);
+  });
 
   it("encadeia todas as ambiguidades de gramas da mesma mensagem", async () => {
diff --git a/server/modules/whatsapp/intent/foodReplacementHandlers.ts b/server/modules/whatsapp/intent/foodReplacementHandlers.ts
index c9ce1f4..0000000 100644
--- a/server/modules/whatsapp/intent/foodReplacementHandlers.ts
+++ b/server/modules/whatsapp/intent/foodReplacementHandlers.ts
@@ -8,7 +8,13 @@ import {
 } from "../replyMessages";
 import { listMeals } from "../../meals/service";
 import type { MealItemInput } from "../../meals/schemas";
