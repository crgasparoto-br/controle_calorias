name: Orchestrator issue 880 corrections

on:
  push:
    branches:
      - feat/875-professional-context-routes
    paths:
      - .github/workflows/orchestrator-issue-880.yml

permissions:
  contents: write

jobs:
  apply-corrections:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply issue 880 corrections
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one anchor, found {count}: {old[:120]!r}")
              file.write_text(text.replace(old, new, 1), encoding="utf-8")

          Path("client/src/lib/professionalPatientDraftStore.ts").write_text(
              '''export type ProfessionalPatientDraftScope = Readonly<{
            patientId: number;
            authorizationId: string;
          }>;

          const professionalPatientDraftSnapshots = new Map<string, unknown>();

          function professionalPatientDraftKey(
            scope: ProfessionalPatientDraftScope | null | undefined
          ) {
            const authorizationId = scope?.authorizationId.trim();
            if (
              !scope ||
              !Number.isSafeInteger(scope.patientId) ||
              scope.patientId <= 0 ||
              !authorizationId
            ) {
              return null;
            }
            return `${authorizationId}:${scope.patientId}`;
          }

          export function readProfessionalPatientDraftSnapshot<T>(
            scope: ProfessionalPatientDraftScope | null | undefined,
            createEmpty: () => T
          ): T {
            const key = professionalPatientDraftKey(scope);
            if (!key) return createEmpty();
            const snapshot = professionalPatientDraftSnapshots.get(key);
            return snapshot === undefined ? createEmpty() : (snapshot as T);
          }

          export function storeProfessionalPatientDraftSnapshot<T>(
            scope: ProfessionalPatientDraftScope | null | undefined,
            snapshot: T
          ) {
            const key = professionalPatientDraftKey(scope);
            if (!key) return;
            professionalPatientDraftSnapshots.set(key, snapshot);
          }

          export function clearProfessionalPatientDraftSnapshot(
            scope: ProfessionalPatientDraftScope | null | undefined
          ) {
            const key = professionalPatientDraftKey(scope);
            if (key) professionalPatientDraftSnapshots.delete(key);
          }

          export function clearProfessionalPatientDraftsForAuthorization(
            authorizationId: string | null | undefined
          ) {
            const normalized = authorizationId?.trim();
            if (!normalized) return;
            const prefix = `${normalized}:`;
            for (const key of professionalPatientDraftSnapshots.keys()) {
              if (key.startsWith(prefix)) {
                professionalPatientDraftSnapshots.delete(key);
              }
            }
          }

          export function clearAllProfessionalPatientDraftSnapshots() {
            professionalPatientDraftSnapshots.clear();
          }
          ''',
              encoding="utf-8",
          )

          Path("client/src/lib/professionalPatientDraftStore.test.ts").write_text(
              '''import { beforeEach, describe, expect, it } from "vitest";
          import {
            clearAllProfessionalPatientDraftSnapshots,
            clearProfessionalPatientDraftsForAuthorization,
            readProfessionalPatientDraftSnapshot,
            storeProfessionalPatientDraftSnapshot,
          } from "./professionalPatientDraftStore";

          beforeEach(clearAllProfessionalPatientDraftSnapshots);

          describe("professional patient draft store", () => {
            it("isolates the same patient across authorization lifecycles", () => {
              const previousAuthorization = {
                patientId: 41,
                authorizationId: "authorization-previous",
              };
              const renewedAuthorization = {
                patientId: 41,
                authorizationId: "authorization-renewed",
              };

              storeProfessionalPatientDraftSnapshot(previousAuthorization, {
                note: "conteúdo sensível",
              });

              expect(
                readProfessionalPatientDraftSnapshot(previousAuthorization, () => ({
                  note: "",
                }))
              ).toEqual({ note: "conteúdo sensível" });
              expect(
                readProfessionalPatientDraftSnapshot(renewedAuthorization, () => ({
                  note: "",
                }))
              ).toEqual({ note: "" });
            });

            it("removes every draft associated with a revoked authorization", () => {
              const firstPatient = {
                patientId: 41,
                authorizationId: "authorization-shared",
              };
              const secondPatient = {
                patientId: 42,
                authorizationId: "authorization-shared",
              };
              storeProfessionalPatientDraftSnapshot(firstPatient, { note: "um" });
              storeProfessionalPatientDraftSnapshot(secondPatient, { note: "dois" });

              clearProfessionalPatientDraftsForAuthorization("authorization-shared");

              expect(
                readProfessionalPatientDraftSnapshot(firstPatient, () => ({ note: "" }))
              ).toEqual({ note: "" });
              expect(
                readProfessionalPatientDraftSnapshot(secondPatient, () => ({ note: "" }))
              ).toEqual({ note: "" });
            });
          });
          ''',
              encoding="utf-8",
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.tsx",
              '''import {
            parseProfessionalPatientRoute,
            professionalPatientPath,
            type ProfessionalPatientSection,
          } from "@/lib/professionalRoutes";
          import { trpc } from "@/lib/trpc";''',
              '''import {
            clearAllProfessionalPatientDraftSnapshots,
            clearProfessionalPatientDraftSnapshot as clearStoredProfessionalPatientDraftSnapshot,
            readProfessionalPatientDraftSnapshot,
            storeProfessionalPatientDraftSnapshot as persistProfessionalPatientDraftSnapshot,
            type ProfessionalPatientDraftScope,
          } from "@/lib/professionalPatientDraftStore";
          import {
            parseProfessionalPatientRoute,
            professionalPatientPath,
            type ProfessionalPatientSection,
          } from "@/lib/professionalRoutes";
          import { trpc } from "@/lib/trpc";''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.tsx",
              '''const patientDraftSnapshots = new Map<number, PatientDraftSnapshot>();

          function createEmptyPatientDraft(): PatientDraftSnapshot {
            return {
              assessment: { ...emptyAssessment },
              note: "",
              guidanceTitle: "",
              guidance: "",
            };
          }

          function getPatientDraftSnapshot(patientId: number) {
            return patientDraftSnapshots.get(patientId) ?? createEmptyPatientDraft();
          }

          function storePatientDraftSnapshot(
            patientId: number,
            snapshot: PatientDraftSnapshot
          ) {
            if (patientId <= 0) return;
            patientDraftSnapshots.set(patientId, {
              ...snapshot,
              assessment: { ...snapshot.assessment },
            });
          }

          export function _forTestOnly_clearProfessionalPatientDraftSnapshots() {
            patientDraftSnapshots.clear();
          }''',
              '''function createEmptyPatientDraft(): PatientDraftSnapshot {
            return {
              assessment: { ...emptyAssessment },
              note: "",
              guidanceTitle: "",
              guidance: "",
            };
          }

          function getPatientDraftSnapshot(
            scope: ProfessionalPatientDraftScope | null
          ) {
            return readProfessionalPatientDraftSnapshot<PatientDraftSnapshot>(
              scope,
              createEmptyPatientDraft
            );
          }

          function storePatientDraftSnapshot(
            scope: ProfessionalPatientDraftScope | null,
            snapshot: PatientDraftSnapshot
          ) {
            persistProfessionalPatientDraftSnapshot(scope, {
              ...snapshot,
              assessment: { ...snapshot.assessment },
            });
          }

          export function _forTestOnly_clearProfessionalPatientDraftSnapshots() {
            clearAllProfessionalPatientDraftSnapshots();
          }''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.tsx",
              '''  const patientId = selectedPatient?.patientId ?? 0;
            const initialDraft = getPatientDraftSnapshot(patientId);
            const [assessment, setAssessment] = useState(initialDraft.assessment);
            const [note, setNote] = useState(initialDraft.note);
            const [guidanceTitle, setGuidanceTitle] = useState(
              initialDraft.guidanceTitle
            );
            const [guidance, setGuidance] = useState(initialDraft.guidance);
            const [transitionReason, setTransitionReason] = useState("");
            const [pages, setPages] = useState<RecordPages>(initialRecordPages);

            const updateAssessment = useCallback<
              React.Dispatch<React.SetStateAction<AssessmentDraft>>
            >(
              nextAssessment => {
                setAssessment(current => {
                  const value =
                    typeof nextAssessment === "function"
                      ? nextAssessment(current)
                      : nextAssessment;
                  const stored = getPatientDraftSnapshot(patientId);
                  storePatientDraftSnapshot(patientId, {
                    ...stored,
                    assessment: value,
                  });
                  return value;
                });
              },
              [patientId]
            );
            const updateNote = useCallback(
              (value: string) => {
                setNote(value);
                storePatientDraftSnapshot(patientId, {
                  ...getPatientDraftSnapshot(patientId),
                  note: value,
                });
              },
              [patientId]
            );
            const updateGuidanceTitle = useCallback(
              (value: string) => {
                setGuidanceTitle(value);
                storePatientDraftSnapshot(patientId, {
                  ...getPatientDraftSnapshot(patientId),
                  guidanceTitle: value,
                });
              },
              [patientId]
            );
            const updateGuidance = useCallback(
              (value: string) => {
                setGuidance(value);
                storePatientDraftSnapshot(patientId, {
                  ...getPatientDraftSnapshot(patientId),
                  guidance: value,
                });
              },
              [patientId]
            );
            const discardDraft = useCallback(() => {
              patientDraftSnapshots.delete(patientId);
              setAssessment({ ...emptyAssessment });
              setNote("");
              setGuidanceTitle("");
              setGuidance("");
            }, [patientId]);
            const clearDraftSection = useCallback(
              (draftSection: "assessment" | "notes" | "guidance") => {
                const stored = getPatientDraftSnapshot(patientId);
                const next =
                  draftSection === "assessment"
                    ? { ...stored, assessment: { ...emptyAssessment } }
                    : draftSection === "notes"
                      ? { ...stored, note: "" }
                      : { ...stored, guidanceTitle: "", guidance: "" };
                storePatientDraftSnapshot(patientId, next);
                if (draftSection === "assessment") setAssessment({ ...emptyAssessment });
                if (draftSection === "notes") setNote("");
                if (draftSection === "guidance") {
                  setGuidanceTitle("");
                  setGuidance("");
                }
              },
              [patientId]
            );
            const setPageForSection = useCallback(
              (targetSection: PaginatedRecordSection, page: number) => {
                setPages(current => ({ ...current, [targetSection]: Math.max(1, page) }));
              },
              []
            );

            useEffect(() => {
              const stored = getPatientDraftSnapshot(patientId);
              setAssessment(stored.assessment);
              setNote(stored.note);
              setGuidanceTitle(stored.guidanceTitle);
              setGuidance(stored.guidance);
              setTransitionReason("");
              setPages(initialRecordPages);
            }, [patientId]);''',
              '''  const patientId = selectedPatient?.patientId ?? 0;
            const authorizationId = selectedPatient?.authorizationId ?? "";
            const draftScope = useMemo<ProfessionalPatientDraftScope | null>(
              () =>
                patientId > 0 && authorizationId
                  ? { patientId, authorizationId }
                  : null,
              [authorizationId, patientId]
            );
            const initialDraft = getPatientDraftSnapshot(draftScope);
            const [assessment, setAssessment] = useState(initialDraft.assessment);
            const [note, setNote] = useState(initialDraft.note);
            const [guidanceTitle, setGuidanceTitle] = useState(
              initialDraft.guidanceTitle
            );
            const [guidance, setGuidance] = useState(initialDraft.guidance);
            const [transitionReason, setTransitionReason] = useState("");
            const [pages, setPages] = useState<RecordPages>(initialRecordPages);

            const updateAssessment = useCallback<
              React.Dispatch<React.SetStateAction<AssessmentDraft>>
            >(
              nextAssessment => {
                setAssessment(current => {
                  const value =
                    typeof nextAssessment === "function"
                      ? nextAssessment(current)
                      : nextAssessment;
                  const stored = getPatientDraftSnapshot(draftScope);
                  storePatientDraftSnapshot(draftScope, {
                    ...stored,
                    assessment: value,
                  });
                  return value;
                });
              },
              [draftScope]
            );
            const updateNote = useCallback(
              (value: string) => {
                setNote(value);
                storePatientDraftSnapshot(draftScope, {
                  ...getPatientDraftSnapshot(draftScope),
                  note: value,
                });
              },
              [draftScope]
            );
            const updateGuidanceTitle = useCallback(
              (value: string) => {
                setGuidanceTitle(value);
                storePatientDraftSnapshot(draftScope, {
                  ...getPatientDraftSnapshot(draftScope),
                  guidanceTitle: value,
                });
              },
              [draftScope]
            );
            const updateGuidance = useCallback(
              (value: string) => {
                setGuidance(value);
                storePatientDraftSnapshot(draftScope, {
                  ...getPatientDraftSnapshot(draftScope),
                  guidance: value,
                });
              },
              [draftScope]
            );
            const discardDraft = useCallback(() => {
              clearStoredProfessionalPatientDraftSnapshot(draftScope);
              setAssessment({ ...emptyAssessment });
              setNote("");
              setGuidanceTitle("");
              setGuidance("");
            }, [draftScope]);
            const clearDraftSection = useCallback(
              (draftSection: "assessment" | "notes" | "guidance") => {
                const stored = getPatientDraftSnapshot(draftScope);
                const next =
                  draftSection === "assessment"
                    ? { ...stored, assessment: { ...emptyAssessment } }
                    : draftSection === "notes"
                      ? { ...stored, note: "" }
                      : { ...stored, guidanceTitle: "", guidance: "" };
                storePatientDraftSnapshot(draftScope, next);
                if (draftSection === "assessment") setAssessment({ ...emptyAssessment });
                if (draftSection === "notes") setNote("");
                if (draftSection === "guidance") {
                  setGuidanceTitle("");
                  setGuidance("");
                }
              },
              [draftScope]
            );
            const setPageForSection = useCallback(
              (targetSection: PaginatedRecordSection, page: number) => {
                setPages(current => ({ ...current, [targetSection]: Math.max(1, page) }));
              },
              []
            );

            useEffect(() => {
              const stored = getPatientDraftSnapshot(draftScope);
              setAssessment(stored.assessment);
              setNote(stored.note);
              setGuidanceTitle(stored.guidanceTitle);
              setGuidance(stored.guidance);
              setTransitionReason("");
              setPages(initialRecordPages);
            }, [draftScope]);''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.tsx",
              '''  const invalidate = async () => {
            await Promise.all([
              utils.professionalRecord.get.invalidate(),
              utils.nutrition.professionals.portfolio.invalidate(),
            ]);
          };
          const saveAssessment = trpc.professionalRecord.saveAssessment.useMutation({
            onSuccess: async () => {
              clearDraftSection("assessment");
              setPageForSection("assessment", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const createNote = trpc.professionalRecord.createNote.useMutation({
            onSuccess: async () => {
              clearDraftSection("notes");
              setPageForSection("notes", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const createGuidance = trpc.professionalRecord.createGuidance.useMutation({
            onSuccess: async () => {
              clearDraftSection("guidance");
              setPageForSection("guidance", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const transitionTracking =
            trpc.professionalRecord.transitionTracking.useMutation({
              onSuccess: async () => {
                setTransitionReason("");
                await invalidate();
              },
            });''',
              '''  const invalidate = async () => {
            await Promise.all([
              utils.professionalRecord.context.invalidate({
                patientId,
                resource: "professional_record",
              }),
              utils.professionalRecord.get.invalidate(),
              utils.nutrition.professionals.portfolio.invalidate(),
            ]);
          };
          const saveAssessment = trpc.professionalRecord.saveAssessment.useMutation({
            onSuccess: async () => {
              clearDraftSection("assessment");
              setPageForSection("assessment", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const createNote = trpc.professionalRecord.createNote.useMutation({
            onSuccess: async () => {
              clearDraftSection("notes");
              setPageForSection("notes", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const createGuidance = trpc.professionalRecord.createGuidance.useMutation({
            onSuccess: async () => {
              clearDraftSection("guidance");
              setPageForSection("guidance", 1);
              guard.markSaved();
              await invalidate();
            },
          });
          const transitionTracking =
            trpc.professionalRecord.transitionTracking.useMutation({
              onSuccess: async (_tracking, variables) => {
                setTransitionReason("");
                if (variables.status === "ended") {
                  discardDraft();
                  setLocation(professionalPatientPath(patientId, "history"));
                }
                await invalidate();
              },
            });''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''import {
            parseProfessionalPatientRoute,
            professionalPatientResourceForRoute,
          } from "@/lib/professionalRoutes";
          import { trpc } from "@/lib/trpc";''',
              '''import {
            clearAllProfessionalPatientDraftSnapshots,
            clearProfessionalPatientDraftsForAuthorization,
          } from "@/lib/professionalPatientDraftStore";
          import {
            parseProfessionalPatientRoute,
            professionalPatientResourceForRoute,
          } from "@/lib/professionalRoutes";
          import { trpc } from "@/lib/trpc";''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''export type ProfessionalPatientContext = {
            patientId: number;
            displayName: string;''',
              '''export type ProfessionalPatientContext = {
            patientId: number;
            authorizationId: string;
            displayName: string;''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  const previousPatientIdRef = useRef<number | null>(null);
            const transitionGenerationRef = useRef(0);''',
              '''  const previousPatientIdRef = useRef<number | null>(null);
            const activeAuthorizationIdRef = useRef<string | null>(null);
            const transitionGenerationRef = useRef(0);''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''    return {
              patientId: patientContext.data.patientId,
              displayName: patientContext.data.displayName,''',
              '''    return {
              patientId: patientContext.data.patientId,
              authorizationId: patientContext.data.authorizationId,
              displayName: patientContext.data.displayName,''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  const clearPatientQueries = useCallback(async () => {''',
              '''  useEffect(() => {
            if (selectedPatient?.authorizationId) {
              activeAuthorizationIdRef.current = selectedPatient.authorizationId;
            }
          }, [selectedPatient?.authorizationId]);

          const clearPatientQueries = useCallback(async () => {''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  const revokePatientContext = useCallback(() => {
            if (revocationInProgressRef.current) return;
            revocationInProgressRef.current = true;
            invalidatePatientContext();
            void clearPatientQueries().finally(() => {
              previousPatientIdRef.current = null;
              setLocation("/professional/patients?notice=patient-access-unavailable");
              revocationInProgressRef.current = false;
            });
          }, [clearPatientQueries, invalidatePatientContext, setLocation]);''',
              '''  const revokePatientContext = useCallback(() => {
            if (revocationInProgressRef.current) return;
            revocationInProgressRef.current = true;
            clearProfessionalPatientDraftsForAuthorization(
              activeAuthorizationIdRef.current
            );
            invalidatePatientContext();
            void clearPatientQueries().finally(() => {
              activeAuthorizationIdRef.current = null;
              previousPatientIdRef.current = null;
              setLocation("/professional/patients?notice=patient-access-unavailable");
              revocationInProgressRef.current = false;
            });
          }, [clearPatientQueries, invalidatePatientContext, setLocation]);''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  const clearPatient = useCallback(() => {
            invalidatePatientContext();
            if (routePatientId ?? previousPatientIdRef.current) {
              void clearPatientQueries();
            }
            if (patientRoute.kind !== "none") {
              setLocation("/professional/patients");
            }
          }, [''',
              '''  const clearPatient = useCallback(() => {
            clearProfessionalPatientDraftsForAuthorization(
              activeAuthorizationIdRef.current
            );
            activeAuthorizationIdRef.current = null;
            invalidatePatientContext();
            if (routePatientId ?? previousPatientIdRef.current) {
              void clearPatientQueries();
            }
            if (patientRoute.kind !== "none") {
              setLocation("/professional/patients");
            }
          }, [''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  useEffect(() => {
            if (routePatientId && profileValidated && !hasActiveProfile) {
              invalidatePatientContext();
              void clearPatientQueries();
            }
          }, [''',
              '''  useEffect(() => {
            if (routePatientId && profileValidated && !hasActiveProfile) {
              clearAllProfessionalPatientDraftSnapshots();
              activeAuthorizationIdRef.current = null;
              invalidatePatientContext();
              void clearPatientQueries();
            }
          }, [''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''  useEffect(() => {
            document.title = `${routeTitle(location)} | Área Profissional`;''',
              '''  useEffect(() => {
            if (!user || (profileValidated && !hasActiveProfile)) {
              clearAllProfessionalPatientDraftSnapshots();
              activeAuthorizationIdRef.current = null;
            }
          }, [hasActiveProfile, profileValidated, user]);

          useEffect(() => {
            document.title = `${routeTitle(location)} | Área Profissional`;''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.tsx",
              '''      transitionGenerationRef.current += 1;
                patientReadyAtRef.current = 0;
                if (previousPatientIdRef.current) void clearPatientQueries();
                previousPatientIdRef.current = null;''',
              '''      transitionGenerationRef.current += 1;
                patientReadyAtRef.current = 0;
                clearAllProfessionalPatientDraftSnapshots();
                activeAuthorizationIdRef.current = null;
                if (previousPatientIdRef.current) void clearPatientQueries();
                previousPatientIdRef.current = null;''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.test.tsx",
              '''} from "./ProfessionalLayout";

          const setLocation = vi.fn();''',
              '''} from "./ProfessionalLayout";
          import {
            clearAllProfessionalPatientDraftSnapshots,
            readProfessionalPatientDraftSnapshot,
            storeProfessionalPatientDraftSnapshot,
          } from "@/lib/professionalPatientDraftStore";

          const setLocation = vi.fn();''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.test.tsx",
              '''beforeEach(() => {
            location = "/professional";''',
              '''beforeEach(() => {
            clearAllProfessionalPatientDraftSnapshots();
            location = "/professional";''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.test.tsx",
              '''  it("removes visible data immediately when the authenticated event stream reports external revocation", async () => {
            location = "/professional/patients/10/reports";
            renderPatientLayout();''',
              '''  it("removes visible data and authorization-scoped drafts when the authenticated event stream reports external revocation", async () => {
            const draftScope = { patientId: 10, authorizationId: "access-10" };
            storeProfessionalPatientDraftSnapshot(draftScope, {
              note: "rascunho sensível",
            });
            location = "/professional/patients/10/reports";
            renderPatientLayout();''',
          )

          replace_once(
              "client/src/components/ProfessionalLayout.test.tsx",
              '''    await waitFor(() => expect(screen.queryByText("Ana")).toBeNull());
            expect(removeQueries).toHaveBeenCalled();
          });

            it("ignores an external revocation event for another patient", async () => {''',
              '''    await waitFor(() => expect(screen.queryByText("Ana")).toBeNull());
            expect(removeQueries).toHaveBeenCalled();
            expect(
              readProfessionalPatientDraftSnapshot(draftScope, () => ({ note: "" }))
            ).toEqual({ note: "" });
          });

            it("ignores an external revocation event for another patient", async () => {''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''let location = "/professional/patients/41/assessment";
          let selectedPatientId = 41;
          let recordData: any;
          const setLocation = vi.fn();
          const getQuery = vi.fn();''',
              '''let location = "/professional/patients/41/assessment";
          let selectedPatientId = 41;
          let selectedAuthorizationId = "authorization-41";
          let recordData: any;
          const setLocation = vi.fn();
          const getQuery = vi.fn();
          const contextInvalidate = vi.fn(async () => undefined);''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''    selectedPatient: {
              patientId: selectedPatientId,
              displayName: `Paciente ${selectedPatientId}`,''',
              '''    selectedPatient: {
              patientId: selectedPatientId,
              authorizationId: selectedAuthorizationId,
              displayName: `Paciente ${selectedPatientId}`,''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''    useUtils: () => ({
              professionalRecord: { get: { invalidate: vi.fn(async () => undefined) } },''',
              '''    useUtils: () => ({
              professionalRecord: {
                context: { invalidate: contextInvalidate },
                get: { invalidate: vi.fn(async () => undefined) },
              },''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''      transitionTracking: { useMutation: mutation },''',
              '''      transitionTracking: {
                useMutation: (options: {
                  onSuccess?: (
                    data: unknown,
                    variables: { status: "active" | "paused" | "ended" }
                  ) => void | Promise<void>;
                }) => ({
                  ...mutation(),
                  mutate: (input: {
                    status: "active" | "paused" | "ended";
                  }) => {
                    void options.onSuccess?.({}, input);
                  },
                }),
              },''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''  selectedPatientId = 41;
            recordData = recordFixture();
            getQuery.mockReset();''',
              '''  selectedPatientId = 41;
            selectedAuthorizationId = "authorization-41";
            recordData = recordFixture();
            contextInvalidate.mockClear();
            getQuery.mockReset();''',
          )

          replace_once(
              "client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx",
              '''  it("clears the preserved draft after an explicit discard", async () => {''',
              '''  it("does not restore a draft when the same patient receives a new authorization", async () => {
            const user = userEvent.setup();
            location = "/professional/patients/41/notes";
            recordData = recordFixture({ notes: [] });
            const view = render(<ProfessionalPatientWorkspace />);

            await user.type(
              screen.getByPlaceholderText(
                "Registre observações internas do acompanhamento."
              ),
              "Rascunho da autorização anterior"
            );
            view.unmount();

            selectedAuthorizationId = "authorization-41-renewed";
            render(<ProfessionalPatientWorkspace />);
            expect(
              (screen.getByPlaceholderText(
                "Registre observações internas do acompanhamento."
              ) as HTMLTextAreaElement).value
            ).toBe("");
          });

          it("redirects immediately to history and refreshes canonical context when tracking ends", async () => {
            const user = userEvent.setup();
            location = "/professional/patients/41";
            recordData = recordFixture();
            render(<ProfessionalPatientWorkspace />);

            await user.click(screen.getByRole("button", { name: "Encerrar" }));

            await waitFor(() =>
              expect(setLocation).toHaveBeenCalledWith(
                "/professional/patients/41/history"
              )
            );
            expect(contextInvalidate).toHaveBeenCalledWith({
              patientId: 41,
              resource: "professional_record",
            });
          });

          it("clears the preserved draft after an explicit discard", async () => {''',
          )

          replace_once(
              "docs/design-docs/professional-navigation.md",
              '''- Rascunhos de avaliação, anotação e orientação são mantidos temporariamente por `patientId` enquanto o workspace está montado. Em navegadores sem Navigation API, um `popstate` cancelado restaura a URL e sincroniza novamente o roteador antes de remontar a rota, sem perder o conteúdo; salvar, descartar explicitamente ou perder a autorização remove o rascunho. O conteúdo nunca é restaurado em outro paciente.
          - A timeline traduz os tipos canônicos suportados, incluindo `tracking_transitioned` e `official_goal_revised`, para rótulos de domínio. Eventos desconhecidos permanecem sanitizados pelo rótulo genérico e não exibem identificadores internos.
          - Acompanhamento `ended` mantém somente `/history`: as demais rotas individuais redirecionam antes de montar conteúdo, e as APIs de prontuário, timezone, dashboard, relatório de período e conversa individual deixam de retornar dados do paciente.''',
              '''- Rascunhos de avaliação, anotação e orientação são mantidos temporariamente pelo par `authorizationId`/`patientId`, impedindo reutilização após revogação e nova autorização da mesma pessoa. Em navegadores sem Navigation API, um `popstate` cancelado restaura a URL e sincroniza novamente o roteador antes de remontar a rota, sem perder o conteúdo. Salvar ou descartar explicitamente remove a seção correspondente; revogação, perda do perfil, encerramento da sessão ou desmontagem do shell eliminam os rascunhos do contexto anterior.
          - A timeline traduz os tipos canônicos suportados, incluindo `tracking_transitioned` e `official_goal_revised`, para rótulos de domínio. Eventos desconhecidos permanecem sanitizados pelo rótulo genérico e não exibem identificadores internos.
          - Acompanhamento `ended` mantém somente `/history`: a conclusão da mutação atualiza o contexto canônico e redireciona imediatamente para o histórico, sem depender do polling ou de recuperar o foco. As demais rotas individuais redirecionam antes de montar conteúdo, e as APIs de prontuário, timezone, dashboard, relatório de período e conversa individual deixam de retornar dados do paciente.''',
          )

          replace_once(
              "docs/PRIVACY_LGPD.md",
              '''- O stream SSE de revogação profissional exige sessão autenticada e autorização vigente na abertura, isola conexões por profissional e paciente e transmite somente `patientId` e instante da revogação; nome, contato, conteúdo clínico, motivo e identificador da autorização não atravessam o stream.

          ## Contexto persistente do WhatsApp''',
              '''- O stream SSE de revogação profissional exige sessão autenticada e autorização vigente na abertura, isola conexões por profissional e paciente e transmite somente `patientId` e instante da revogação; nome, contato, conteúdo clínico, motivo e identificador da autorização não atravessam o stream.
          - Rascunhos profissionais não salvos permanecem somente em memória e são isolados pelo identificador da autorização e do paciente. Revogação, perda do perfil profissional, encerramento da sessão ou desmontagem do shell removem esse conteúdo antes que outro contexto possa ser exibido.

          ## Contexto persistente do WhatsApp''',
          )

          Path(".github/workflows/orchestrator-issue-880.yml").unlink()
          PY

      - name: Format changed files
        run: |
          pnpm exec prettier --write \
            client/src/lib/professionalPatientDraftStore.ts \
            client/src/lib/professionalPatientDraftStore.test.ts \
            client/src/pages/professional/ProfessionalPatientWorkspace.tsx \
            client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx \
            client/src/components/ProfessionalLayout.tsx \
            client/src/components/ProfessionalLayout.test.tsx \
            docs/design-docs/professional-navigation.md \
            docs/PRIVACY_LGPD.md

      - name: Run focused regression tests
        run: |
          pnpm exec vitest run \
            client/src/lib/professionalPatientDraftStore.test.ts \
            client/src/pages/professional/ProfessionalPatientWorkspace.auditCorrections.test.tsx \
            client/src/components/ProfessionalLayout.test.tsx \
            client/src/pages/professional/ProfessionalPatientRouteGuard.test.tsx

      - name: Run type and documentation checks
        run: |
          pnpm check
          pnpm docs:check

      - name: Commit and push corrections
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix: close issue 880 audit findings"
          git push origin "HEAD:${GITHUB_REF_NAME}"
