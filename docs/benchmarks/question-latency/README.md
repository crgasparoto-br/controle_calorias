# Benchmark de latência QUESTION

O protocolo canônico atual é o **v4**. Consulte [README-v4.md](README-v4.md) para boundary produtivo, semântica das métricas, gates e execução exact-SHA.

A documentação do protocolo anterior foi preservada em [README-v3.md](README-v3.md).

A coorte continua versionada em `fixtures/manifest.json` e os resultados devem ser gravados em `results/`. Evidência quantitativa só é válida quando `candidateSha` é exatamente o material head congelado.
