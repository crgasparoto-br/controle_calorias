# Issue #1096 — auditoria de reachability dos bridges do WhatsApp

## Conclusão da implementação

A implementação da Fase 3 foi iniciada sobre o SHA-base de `develop` `b3156bcf28cf854b14a657f4750a3f78b2a5ac67`. O commit de implementação auditado é `24cac382d9aa9e5b0c7a5f79dd94c70f10d56728`, com o follow-up documental `a95a5c54c290f7a6aeffe7bb3b1bb892b5b560c3`. O SHA de referência da Fase 0 é `4d86cb814ff57164ff16cd7626ea21be46719fce`. O snapshot atual auditado de `develop` é `394337d1166c4d12df3c78010246f65a63037eee`, capturado em `2026-09-17T13:51:57Z` por `git rev-parse origin/develop` antes desta correção. Os findings foram revalidados contra esse snapshot: **F0-05 permanece `keep`**, enquanto **F0-06 e F0-08 permanecem `defer`** porque a cadeia real ainda possui responsabilidades distintas e contratos de compatibilidade legítimos.

O conjunto de remoções aprovado é vazio. Nenhum wrapper foi removido por delegação fina, nome semelhante, cobertura baixa ou busca textual sem referências. Essa é uma decisão de segurança: a evidência disponível demonstra reachability e responsabilidade própria para todos os bridges candidatos. Remover qualquer um deles nesta revisão reduziria lifecycle, precedência, telemetria, compatibilidade ou tratamento de mídia sem substituto equivalente comprovado.

A cadeia produtiva continua sendo:

```text
POST /api/whatsapp/webhook
  -> registerWhatsAppPublicPostRoute
     -> handleWhatsAppPersistentContextWebhook
        -> handleWhatsAppWebhookWithImageIdempotency
           -> handleWhatsAppWebhookWithTextIntent
              -> handleWhatsAppWebhookWithAnnotatedImages
                 -> handleWhatsAppWebhook
                    -> handleWhatsAppWebhookImplementation
```

O fluxo acima é exercitado pelo POST HTTP real na caracterização da #1094. A verificação estática adicionada nesta issue funciona apenas como uma barreira complementar contra remoções ou bypasses acidentais; ela não substitui os golden flows. A suíte adicional `issue1096.bridgeReachability.runtime.test.ts` executa o POST real até o limite do wrapper de idempotência com o handler produtivo anterior, e as suítes `issue1096.bridgeReachability.downstream.runtime.test.ts` e `issue1096.bridgeReachability.fallback.runtime.test.ts` executam as delegações de intenção, anotação e fallback com doubles determinísticos para impedir efeitos externos.

## Revalidação dos findings

| Finding                                     | Decisão nesta fase | Evidência e consequência                                                                                                                                                                                                                                |
| ------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F0-05 — fachada `server/whatsappWebhook.ts` | `keep`             | A fachada exporta a assinatura histórica, fornece a verificação do webhook e é o fallback nutricional chamado pela implementação de imagem. Ela não é o primeiro handler HTTP, mas continua sendo um consumidor produtivo e uma API de compatibilidade. |
| F0-06 — implementações paralelas de webhook | `defer`            | A cadeia possui gates com precedência diferente: contexto persistente, claim de processamento, intenção textual, imagem anotada e fallback geral. Os golden flows atravessam a composição pública e não demonstram equivalência para fundi-los.         |
| F0-08 — código morto ou bridge seguro       | `defer`            | A reachability produtiva está fechada para os wrappers listados abaixo. Os dois adaptadores históricos de #1095 ainda têm condições de aposentadoria explícitas e não devem ser apagados por inspeção local.                                            |

## Inventário de reachability, owners e aposentadoria

A coluna **Owner** identifica o módulo que pode decidir ou alterar o fato. Um wrapper pode transportar a decisão de um owner sem ser código morto. A condição de aposentadoria é objetiva e não é satisfeita apenas por ausência de referências textuais.

| Símbolo                                                                                           | Owner                                                                                                                                                              | Responsabilidade concreta                                                                                                                                           | Consumidor produtivo e alcance                                                                                                                                                            | Substituto canônico                                                                                                 | Decisão e condição de aposentadoria                                                                                                                                                         | Golden flow ou teste discriminante                                                                                |
| ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `server/whatsappPublicRoute.ts::registerWhatsAppPublicPostRoute`                                  | Runtime HTTP em `server/_core/index.ts`                                                                                                                            | Registra o POST público, parser, rate limit, correlação de ingresso e resposta retryable para erros assíncronos.                                                    | `server/_core/index.ts` registra a rota oficial `/api/whatsapp/webhook`.                                                                                                                  | Não há outro registro público equivalente.                                                                          | `keep`. Só pode mudar se o bootstrap continuar registrando exatamente o mesmo contrato HTTP e os testes do POST real permanecerem verdes.                                                   | `whatsappWebhook.issue1094.characterization.test.ts`, que cria Express, registra esta rota e envia o POST.        |
| `server/whatsappPersistentContextWebhook.ts::handleWhatsAppPersistentContextWebhook`              | `messageLifecycle`, `timeZoneContext`, `storagePersistenceCorrelation` e `whatsappBillingWriteGate`                                                                | Aplica gate comercial, escopos de lifecycle/timezone, correlação de storage e enriquecimento da mesma mensagem inbound.                                             | `server/_core/index.ts` passa este handler à rota pública.                                                                                                                                | Não há substituto que preserve os escopos e a correlação antes do claim.                                            | `keep`. A aposentadoria exige migrar explicitamente gate, correlação e escopos para um único entrypoint e repetir a matriz pública.                                                         | Golden flows da #1094 e testes de restart/idempotência do lifecycle.                                              |
| `server/whatsappImageIdempotencyWebhook.ts::handleWhatsAppWebhookWithImageIdempotency`            | `messageLifecycle` para claim; o wrapper é owner da precedência de onboarding, billing e hidratação de imagem                                                      | Faz claim persistente, absorve duplicata/inflight/unavailable, trata onboarding, billing e água em imagem antes do pipeline pesado e propaga contexto de progresso. | Importado diretamente por `whatsappPersistentContextWebhook.ts`; é alcançado a partir do POST produtivo.                                                                                  | `messageLifecycle` fornece os owners de persistência, mas não substitui a precedência de canal, onboarding ou água. | `keep`. A aposentadoria somente é possível após migrar cada caso para um entrypoint único, demonstrar replay antes de efeitos caros e remover os testes de compatibilidade correspondentes. | `whatsappImageIdempotencyWebhook.test.ts`, `processingOwnership.test.ts` e cenários públicos de #1094.            |
| `server/whatsappIntentWebhook.ts::handleWhatsAppWebhookWithTextIntent`                            | `messageRouter` e handlers de domínio de intenção; o wrapper é owner da precedência e do transporte para o fallback                                                | Resolve callbacks, pendências, comandos, água/peso, adições e o gate contável antes do fallback nutricional; filtra mensagens e transporta hints estruturados.      | Importado por `whatsappImageIdempotencyWebhook.ts` e chamado para as mensagens restantes após o claim.                                                                                    | `messageRouter` e handlers de domínio são owners internos, mas não substituem a composição/precedência do wrapper.  | `keep`. A aposentadoria exige provar pelo POST real a mesma precedência, idempotência de inbound, ausência de fallback indevido e contagens não maiores.                                    | `whatsappIntentWebhook.test.ts`, regressões de callbacks e a caracterização pública da #1094.                     |
| `server/whatsappAnnotatedImageWebhook.ts::handleWhatsAppWebhookWithAnnotatedImages`               | `imageAnnotationTelemetryContext` para o contexto; `annotatedImage` para o resultado da anotação                                                                   | Mantém o contexto de telemetria da anotação e expõe o contrato de compatibilidade para o fluxo de imagem.                                                           | Importado por `whatsappIntentWebhook.ts`.                                                                                                                                                 | `imageAnnotationTelemetryContext` e `annotatedImage` são owners de partes internas, não do limite do wrapper.       | `keep`. O contexto não pode ser removido enquanto `annotatedImage.ts` registrar o resultado e `db.ts` normalizar eventos a partir dele.                                                     | `whatsappAnnotatedImageWebhook.test.ts` e `issue925ImageAnnotationTelemetryRegression.test.ts`.                   |
| `server/whatsappAnnotatedImageWebhookImplementation.ts::handleWhatsAppWebhookWithAnnotatedImages` | `webhookMediaPipeline`/storage para mídia, `MEAL_VISION` para análise e `annotatedImage` para o artefato auxiliar                                                  | Baixa e persiste mídia, usa imagem inline, executa visão, trata preferência/anotação, mantém falha de auxiliar não bloqueante e delega payload restante.            | Chamado pela fachada de imagem; seu fallback chama `server/whatsappWebhook.ts::handleWhatsAppWebhook`.                                                                                    | `annotatedImage.ts` só substitui a geração do artefato; não substitui download, persistência, lifecycle e fallback. | `keep`. Só pode ser consolidado após demonstrar equivalência para imagem pura, legenda operacional, falha de storage, falha de anotação e payload multimodal.                               | Toda a matriz de imagem da #1094 e `whatsappAnnotatedImageWebhook.test.ts`.                                       |
| `server/whatsappWebhook.ts::handleWhatsAppWebhook`                                                | `whatsappWebhookImplementation` para processamento final; `questionLatencyContext` e `imageAnnotationTelemetryContext` para escopos; runtime HTTP para verificação | Mantém a API de compatibilidade, verifica o token e envolve a implementação final com contextos de latência e telemetria; é o fallback do wrapper de imagem.        | `server/_core/index.ts` usa `verifyWhatsAppWebhook`; `whatsappAnnotatedImageWebhookImplementation.ts` chama `handleWhatsAppWebhook`; testes e consumidores históricos importam a fachada. | `whatsappWebhookImplementation.ts` é a implementação, não substituto da fachada pública.                            | `keep` conforme F0-05. A condição de aposentadoria é zero consumidores produtivos, zero consumidores de compatibilidade e migração do fallback com equivalência demonstrada no POST real.   | `whatsappWebhook.test.ts`, `whatsappWebhook.secret.test.ts`, golden flows de texto/áudio/imagem.                  |
| `server/whatsappWebhookImplementation.ts::handleWhatsAppWebhook`                                  | `nutritionEngine`, persistência de refeições, `mealConsolidationService`, `messageLifecycle` e `logicalReplyDelivery`                                              | Executa fallback nutricional, processamento de mídia preparada, persistência, consolidação, lifecycle e resposta lógica.                                            | Chamado pela fachada principal e alcançado pelo ramo de fallback do processamento de imagem.                                                                                              | Owners internos não substituem esta orquestração final.                                                             | `keep`. Só pode ser extraído em blocos menores com substituto e contadores equivalentes; não é código morto.                                                                                | `whatsappWebhook.test.ts`, `whatsappWebhook.audioTranscription.test.ts`, `whatsappWebhook.image.test.ts` e #1094. |

A dupla chamada de `runWithMessageLifecycleRequestScope` e de `runWithQuestionLatencyContext` não constitui duplicação de decisão. Os escopos detectam um contexto ativo e executam a operação dentro do owner já existente. A camada externa é necessária no entrypoint público; a camada da fachada preserva consumidores diretos históricos. Essa propriedade é protegida pelo teste de composição e pelos testes de lifecycle.

## Adaptadores históricos de #1095

`recoverCanonicalCommercialIdentity` permanece exportado apenas para compatibilidade externa. O consumidor produtivo interno foi migrado para `resolveStructuredCommercialIdentity`; sua aposentadoria exige uma varredura externa com zero consumidores da assinatura histórica, seguida da remoção do export e da matriz de regressão correspondente.

`prepareCountableFoodRegistration` permanece como adaptador síncrono histórico. O owner produtivo é `prepareCountableFoodRegistrationResolved`, usado pelo gate WhatsApp. A aposentadoria exige zero chamadas síncronas após a varredura do repositório, package e consumidores externos conhecidos, além da migração da suíte #997. A busca atual encontra consumidores de teste e compatibilidade; portanto, a remoção não foi aprovada.

## Contagens e comportamento preservado

A caracterização pública da #1094 permanece com **17/17 testes aprovados**. A regressão focada da #1095 permanece com **12/12 testes aprovados**. Os testes dos wrappers e intents executados nesta fase permanecem com **54/54 testes aprovados**. A regressão explícita de #1072 permanece com **30/30 testes aprovados**, distribuídos entre `commercialServingRelation.issue1072.test.ts` (15), `countableFoodQuantity.issue1072.searchContext.test.ts` (1), `countableFoodRegistrationGate.issue1072.test.ts` (5) e `nutritionSearchDecisionTelemetry.issue1072.test.ts` (9). O typecheck (`pnpm check`) permanece verde. Os valores observados por cenário e a comparação before/after estrutural estão em [`issue-1096-metrics.md`](./issue-1096-metrics.md); a execução registrada totalizou 16 linhas de evidência para 17 testes da caracterização, sem aumento introduzido pela Fase 3.

Esta Fase 3 não alterou código de produção: o diff funcional contém somente documentação e o teste complementar de reachability. Por isso, o delta operacional desta fase é **zero** para `processMealInput`, `NUTRITION_SEARCH`, persistências, claims e round-trips. A caracterização pública pós-implementação foi executada novamente para confirmar que a matriz da #1094 continua verde; ela mantém o limite de uma operação outbound de `NUTRITION_SEARCH` por item e a monotonicidade das decisões estruturadas até a persistência. A comparação code-v-code da Fase 2 permanece registrada em `docs/testing/issue-1095-ownership-consolidation.md`.

## Evidência executável

O teste `server/issue1096.bridgeReachability.test.ts` verifica a composição documentada, os consumidores produtivos dos wrappers, a preservação da fachada F0-05 e a existência dos golden flows públicos. Ele é deliberadamente complementar: a prova comportamental continua sendo o POST real de `server/whatsappWebhook.issue1094.characterization.test.ts`. Os testes runtime da composição observam os limites produtivos sem depender de busca textual como única evidência.

Comandos de revalidação:

```text
pnpm vitest run server/issue1096.bridgeReachability.test.ts --reporter=dot
pnpm vitest run server/issue1096.bridgeReachability.runtime.test.ts server/issue1096.bridgeReachability.downstream.runtime.test.ts server/issue1096.bridgeReachability.fallback.runtime.test.ts --reporter=dot
pnpm vitest run server/whatsappWebhook.issue1094.characterization.test.ts --reporter=dot
pnpm vitest run server/issue1095.ownershipConsolidation.test.ts server/modules/whatsapp/confirmedMealRegistration.issue1095.test.ts server/modules/whatsapp/intent/canonicalFoodAdditionResolution.issue1016.test.ts --reporter=dot
pnpm vitest run server/commercialServingRelation.issue1072.test.ts server/countableFoodQuantity.issue1072.searchContext.test.ts server/modules/whatsapp/countableFoodRegistrationGate.issue1072.test.ts server/nutritionSearchDecisionTelemetry.issue1072.test.ts --reporter=dot
pnpm vitest run server/whatsappImageIdempotencyWebhook.test.ts server/whatsappImageIdempotencyWebhook.processingOwnership.test.ts server/whatsappAnnotatedImageWebhook.test.ts server/whatsappIntentWebhook.test.ts server/whatsappWebhook.test.ts --reporter=dot
pnpm check
```

## Decisão da implementação

O registro desta implementação mantém todos os wrappers, porque cada candidato foi alcançado por entrypoints produtivos ou preserva contratos históricos e cada tentativa de simplificação sem substituto equivalente violaria os controles da issue. A aposentadoria futura é condicional, não automática: somente após os consumidores síncronos/externos históricos serem migrados e uma nova matriz pública provar equivalência.

O parecer independente do ciclo é mantido separado desta decisão de implementação em [`issue-1096-independent-audit.md`](./issue-1096-independent-audit.md). A aprovação final somente deve ser registrada depois que a auditoria independente confirmar que os requisitos e as correções documentais estão completos.

## Referências

[1]: ../../ARCHITECTURE.md "Arquitetura do Controle de Calorias"
[2]: ../design-docs/whatsapp-ingestion.md "Design técnico: ingestão WhatsApp"
[3]: ../design-docs/nutrition-engine.md "Design técnico: motor nutricional"
[4]: ./issue-1095-ownership-consolidation.md "Issue #1095 — consolidação de owners de identidade, medida e contrato"
